export class address {

    number: number;
    street: string;
    city: string;
    postcode: number;

    constructor(number: number, street: string, city: string, postcode: number) {
        this.number = number;
        this.street = street;
        this.city = city;
        this.postcode = postcode;
    }

}